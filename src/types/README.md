This folder should only contains global types definitions

Component specific types should be defined inside component definition tsx file (/src/ui/components folder)

Data / Backend types should be defined inside /src/lib/data/types folder
